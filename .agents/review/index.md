# Review status

Workflow: see `.agents/playbooks/codereview.md`.
Per-finding detail: see `.agents/review/findings/<id>.md`.

## Legend
- `[ ]` Admitted, open (passed intake triage; not yet started)
- `[~]` In progress / pending review
- `[x]` Verified (awaiting owner-gated merge)
- `[!]` Contested — declined, disputed, or ruled invalid; awaiting owner adjudication
- `[-]` Declined at intake (kept for the record; no work)

## Findings

| ID     | Severity | Impact (one line)                                        | Status | Branch | Reviewer |
|--------|----------|----------------------------------------------------------|--------|--------|----------|
| slice0  | MEDIUM   | Wrong capacity harness → wrong D1 caps (evidence only)   | `[x]`  | —      | codex (owner-run interactive, default model+effort, workspace-write) — accepted, guard_confirmed |
| slice-a | LOW      | CSV UI surface survives parking, or endpoint removed with it | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (bypass-sandbox, owner-authorized) — accepted, guard_confirmed |
| slice-b1 | MEDIUM  | Miswired headline classifier → wrong/misleading answer or DATA-D1 breach | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| slice-t1 | MEDIUM  | Vacuous browser harness → false green for every downstream front-end slice | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| slice-b2 | MEDIUM  | Broken render branch or misapplied theme → wrong/unreadable headline answer | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — R1 **reopened** (incomplete theme migration), repaired `08cb19b`, R2 accepted, guard_confirmed |
| slice-c1 | MEDIUM  | Mis-sized/unenforced follow-up cap → over-limit context persisted/logged/transmitted, or corrupt fragment | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — R1 **reopened** (retry enqueue path unenforced + chained directives), repaired `8549ecc`, R2 accepted, guard_confirmed |
| slice-c2 | MEDIUM  | Client-asserted follow-up provenance/context → foreign last-turn leak or unbounded multi-turn injection (FOLLOWUP-D2) | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| slice-c3 | MEDIUM  | Chat display-log transmitted (FOLLOWUP-D2 breach) or broken resize-clamp/exchange delineation (Design contract) | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| sync-d1 | MEDIUM  | Over-removal: retiring sync `execute` strips the shipped async route or a shared helper → broken query path | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| csv-kill-d1 | MEDIUM  | Over-removal: killing CSV enrichment strips the surviving query/download path or breaks the warnings-as-errors build | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| reqbody-d1 | MEDIUM  | Body cap unwired/mis-targeted → raw front-door unbounded (memory exhaustion) or legitimate requests 413'd | `[x]`  | —      | codex/@azure-openai-eus2-global/gpt-5.5-dzs/xhigh/standard (`--profile review`, owner-authorized) — accepted, guard_confirmed |
| f04-or-1 | HIGH     | Dropping the RAM cache before migrating preview + single-record headline → 404s and downgraded answers | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-2 | HIGH     | Membership-only reuse key serves a prior projection's columns/filters as this turn's answer | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-3 | MEDIUM   | Narrate reduction lacks distribution shape → fabricated or misleading "most common value" answer | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-4 | HIGH     | Case-fold specified on `ToDictionary` instead of `GroupBy` → duplicate-key throw on every case-variant bucket | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-5 | MEDIUM   | `not_contains`/`not_starts_with`/`not_ends_with` with an empty value match nothing → silent empty result | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-6 | HIGH     | 32 KB backstop < a valid max-length question thread → "unreachable" error path reachable in-bounds | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-7 | HIGH     | Completion artifact has no lifecycle → AD data accumulates until `E:\WWWOutput` fills | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted, plan amended |
| f04-or-8 | MEDIUM   | Decisions log claims unlanded F04 guards landed and verified → cold session skips writing them | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted, records corrected |
| slice1-or-1 | HIGH  | `group_by` names an attribute but rows are keyed by projection column name → aliased grouping folds every record into one fabricated `(empty)` bucket | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview, inline session-only, `codex-commercial.ps1`) — admitted, guard_confirmed |
| slice1-or-2 | MEDIUM | Composite group keys join and split on an unescaped `\|` → distinct buckets merge and export columns shift | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview, inline session-only, `codex-commercial.ps1`) — admitted (remedy scope disputed; structural change deferred to Slice 7), guard_confirmed |
| slice1r2-or-1 | HIGH | Grouping reads the display projection → a plan grouping on an unprojected attribute reports one fabricated `(empty)` bucket as the answer | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted (fail-validation fallback declined; valid plan shape), guard_confirmed |
| slice1r2-or-2 | MEDIUM | Escaped composite keys forwarded verbatim to the headline and follow-up context → user and model shown altered directory values | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview r2, inline session-only, `codex-commercial.ps1`) — admitted, guard_confirmed |
| slice5-or-1 | HIGH | `Enabled`/`AccountExpirationDate` special cases run before the populated-attribute branch → `not_equals ""` returns a wrong subset at the LDAP layer, unrecoverable in-memory | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 5 r1, inline session-only, `codex-commercial.ps1`) — **envelope contract FAILED** (prose, no JSON verdict); findings extracted, admitted at intake; fixed, guard proven in two halves (7 red + 2 red) |
| slice5-or-2 | MEDIUM | `AccountExpirationDate equals ""` is admitted by the validator but compared literally against the synthesized `"Never"` → silently discards every intended match | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 5 r1, inline session-only, `codex-commercial.ps1`) — **envelope contract FAILED** (prose, no JSON verdict); findings extracted, admitted at intake; fixed, guard proven (2 red) |
| slice3-or-1 | MEDIUM | Alternate-model retry moves the panel and the follow-up reference to the new job but leaves the chat showing the rejected answer | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 3 r1, inline session-only, `codex-commercial.ps1`) — envelope schema-valid, both SHAs matched the dispatch; admitted (remedy is the reviewer's stated fallback; the appended-second-turn option declined), guard_confirmed |
| slice4-or-1 | MEDIUM | Export rule enforced only in the browser → a one-line or single-record answer still serves its rows over the advertised download URL | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 4 r1, inline session-only, `codex-commercial.ps1`) — envelope schema-valid, both SHAs matched the dispatch; admitted, remedy as the reviewer stated it, guard proven in two halves (2 red + 1 red) |
| slice4-or-2 | MEDIUM | Invariant lock stops at interface bodies and names two fields literally → an extracted export service could reintroduce model calls while the guard stays green | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 4 r1, inline session-only, `codex-commercial.ps1`) — admitted (endpoint-test remedy declined: the configurable output root is Slice 7's scope; provenance guarded at the serializer instead), guard proven by reproducing the hidden-DI-call scenario, then reddening it |
| slice6-or-1 | MEDIUM | Alternate-model retry clones a turn without its `PreviousJobId` → the replacement becomes a thread root and the next follow-up loses the established subject | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 6 r1, inline session-only, `codex-commercial.ps1`) — envelope schema-valid, both SHAs matched the dispatch; admitted, remedy as the reviewer stated it, guard_confirmed |
| slice6-or-2 | MEDIUM | The scoping block lists bare "instead of..." as a subject reset → a constraint replacement licenses the directory-wide escape the block exists to prevent | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 6 r1, inline session-only, `codex-commercial.ps1`) — envelope schema-valid, both SHAs matched the dispatch; admitted against my own Slice 6c wording; guard proven in two halves (1 red + 1 red) |
| slice7-or-1 | HIGH | A swallowed artifact write still completes the job → Completed with no readable result anywhere, since this slice deleted the cache fallback its own comment promises | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 7 r1, inline session-only, `codex-commercial.ps1`) — **envelope contract FAILED** (prose, no JSON), recovered by one re-emission-only re-prompt: schema-valid, both SHAs matched; admitted, fixed, guard proven (1 red) |
| slice7-or-2 | MEDIUM | Reuse reads an ancestor artifact it has not claimed → the concurrent retention sweep can delete it, leaving a Completed job pointing at nothing | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 7 r1, inline session-only, `codex-commercial.ps1`) — envelope recovered as above; admitted, fixed, guard proven (1 red) |
| slice7-or-3 | MEDIUM | An interrupted artifact write leaks its temp file until the next restart → the volume the admission check protects fills with unreclaimable partials | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 7 r1, inline session-only, `codex-commercial.ps1`) — envelope recovered as above; admitted, fixed, guard proven (1 red) |
| slice7-or-4 | LOW | The reduction ladder's bottom two rungs carry no result evidence → under a small configured cap, Narrate answers a question it was given no facts about | `[x]`  | —      | codex/gpt-5.6-sol/xhigh/frontier (openreview Slice 7 r1, inline session-only, `codex-commercial.ps1`) — envelope recovered as above; admitted (pre-existing Slice 2 code; framing disputed, remedy as stated), fixed, guard proven (1 red) |
